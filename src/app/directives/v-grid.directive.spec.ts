import { VGridDirective } from './v-grid.directive';

describe('VGridDirective', () => {
  it('should create an instance', () => {
    const directive = new VGridDirective();
    expect(directive).toBeTruthy();
  });
});
