import { BindCssDirective } from './bind-css.directive';

describe('BindCssDirective', () => {
  it('should create an instance', () => {
    const directive = new BindCssDirective();
    expect(directive).toBeTruthy();
  });
});
